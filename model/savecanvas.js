// Schema for saving canvas NOT USED!!!!
/*
var mongoose 		= require('mongoose');
var User       		= require('../model/user');

var savecanvasSchema = mongoose.Schema({

    canvas            : {
    	name	: String,	
        data    : String
         Describes who has saved the drawing, the name of the drawing and all data within the drawing
    }
});

// create the model for saving canvas and expose it to our app
module.exports = mongoose.model('savecanvas', savecanvasSchema);*/